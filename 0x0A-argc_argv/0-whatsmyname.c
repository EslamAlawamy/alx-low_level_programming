#include <stdio.h>
/**
 * main - the main fun
 * @argc: argument count
 * @argv: argument vector
 * Return: is 0
 */
int main(int argc, char *argv[])
{
        printf("%s\n", argv[agrc - 1]);
        return (0);
}

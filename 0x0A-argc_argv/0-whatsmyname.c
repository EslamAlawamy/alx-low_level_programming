#include <stdio.h>
/**
 * main - the main fun
 * @argc: argument count
 * @argv: argument vector
 * Return: is 0
 */
int main(int argc __attribute__((unused)), char *argv[])
{
        printf("%s\n", argv[]);
        return (0);
}
